################################
# 1. Visual Studio Code / IDE
################################
.vscode/
.idea/
*.swp
*.bak
.DS_Store
Thumbs.db

################################
# 2. Rust Application (rust-api/)
################################
# Standard Rust build output directory
/rust-api/target/

# Cargo metadata and lock files created by the user/IDE that shouldn't be shared.
/rust-api/Cargo.lock

# Environment secrets and config files (CRITICAL for security/portability)
/rust-api/.env
/rust-api/config.toml

################################
# 3. Docker Environment
################################
# Docker build context temporary files
**/.dockerignore

# Logs and temporary files generated by Docker
/docker-compose.override.yml  # Often used for local developer overrides
/tmp/*

################################
# 4. Appsmith State (CRITICAL: DO NOT COMMIT DATA)
################################
# This directory holds all the persistent data, configuration,
# and internal databases (MongoDB/Redis) for your Appsmith instance.
# **Committing this data to Git will break portability and expose credentials.**
/appsmith_stacks

# Any temporary compilation or debugging information
/target